/**
 * Proveedor — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `nuevo-proveedor.view.ts`,
 * datos en `use-nuevo-proveedor.ts`, lógica custom en `handlers.ts`.
 */
export { NuevoProveedorView } from './nuevo-proveedor.view.js';
