/**
 * Proveedores — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `proveedores.view.ts`,
 * datos en `use-proveedores.ts`, lógica custom en `handlers.ts`.
 */
export { ProveedoresView } from './proveedores.view.js';
